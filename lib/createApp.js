"use strict"
const AWS = require('aws-sdk')
const express = require('express')
const app = express()
module.exports = ({
    config
}) => {

    async function configure() {

    }

    async function run() {

    }
}